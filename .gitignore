# ── Credenciales (NUNCA subir) ──
include/credentials.h

# ── Build artifacts (PlatformIO) ──
.pio/
compile_commands.json

# ── IDE / Editor ──
.vscode/.browse.c_cpp.db*
.vscode/c_cpp_properties.json
.vscode/launch.json
.vscode/ipch
.vscode/settings.json
*.code-workspace

# ── Backups y temporales ──
*.bak
*~
__history/
*.tmp
*.swp

# ── Scratch (contiene credenciales de otras redes) ──
scratch/

# ── ProyectoDeAutomatizacion (otro proyecto, maquinas personales) ──
ProyectoDeAutomatizacion/

# ── Tools locales ──
.agents/
.cache/
.opencode/*
!.opencode/skills/
.codebase-memory/
codebase-memory-mcp/

# ── Archivos comprimidos ──
*.rar
*.zip
*.tar.gz

# ── OS ──
.DS_Store
Thumbs.db
